using System.Windows;

namespace EverythingImages;

/// <summary>The application object. Everything happens in MainWindow and the
/// AppState it owns; this is here because App.xaml needs its other half.</summary>
public partial class App : Application
{
}
